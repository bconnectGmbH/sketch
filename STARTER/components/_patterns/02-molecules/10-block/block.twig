{#
/**
 * Block Default
 *
 * Standard Drupal Block Template
 *
 * @param string $title_prefix Zusätzliche Ausgabe durch Modulen oder Elementen die vor dem Titel ausgegeben werden sollen.
 * @param array $title_attribute Attribute die der Titel bekommen soll
 * @param string $label Block Titel des Blocks
 * @param string $title_suffix Zusätzliche Ausgabe durch Modulen oder Elementen die nach dem Titel ausgegeben werden sollen.
 * @param string $content Inhalt des Blocks
 * 
 *
 * @example
 * {% embed "@molecules/10-block/block.twig" with {
 *   label: "Default Block"
 * } %}
 *   {% block content %}
 *     <p>London is the capital city of England. It is the most populous city in the United Kingdom,
 *     with a metropolitan area of over 13 million inhabitants.</p>
 *     <p>Standing on the River Thames, London has been a major settlement for two millennia,
 *     its history going back to its founding by the Romans, who named it Londinium.</p>
 *     <p><strong>Resize this page to see what happens!</strong></p>
 *   {% endblock %}
 * {% endembed %}
 */
#}

<div class="block">
  {{ title_prefix }}
  {%- if label -%}
    <h2{{ title_attribute }}>{{ label }}</h2>
  {%- endif -%}
  {{ title_suffix }}
  {% block content %}
    {{ content }}
  {% endblock %}
</div>
